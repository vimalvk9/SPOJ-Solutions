#include <bits/stdc++.h>
using namespace std;

typedef unsigned long long ll;
int main()
{
	int budget , parties;
	int fee[505],fun[505];

    cin >> budget >> parties;

    while(budget != 0 && parties != 0)
     {
     	int i , x , y;

     	for(i=0;i<parties;i++)
         cin >> fee[i] >> fun[i];
        cout << "Yo\n";
        cin >> budget >> parties;
     }  

	return 0;  
}
